import {
    queryViewer,
    queryRepositoryDirectories,
} from './queries';

import {
    Viewer,
    FileEntries,
    RepositoryDirectories,
} from './types';

export {
    // export queries
    queryViewer,
    queryRepositoryDirectories,

    // export query return types
    Viewer,
    FileEntries,
    RepositoryDirectories,
 };
